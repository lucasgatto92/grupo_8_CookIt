<!DOCTYPE html>
<html lang="en">
<%- include('partials/head') %>

    <body>
        <%- include('partials/header') %>
            <div id="fondoRegister">
                <div id="logreg-forms">
                    <form action="/users/registro" class="form-signup" method="POST" enctype="multipart/form-data">
                        <small class="text-danger"><%= (typeof errors != 'undefined' && errors.nombre) ? errors.nombre.msg : '' %></small>

                        <input type="text" id="user-name" class="form-control" autofocus name="nombre" value="<%=(typeof old != 'undefined' && old.nombre)?old.nombre:'' %>" placeholder="Nombre">

                        <small class="text-danger"><%= (typeof errors != 'undefined' && errors.apellido) ? errors.apellido.msg : '' %></small>

                        <input type="text" id="user-lastname" class="form-control" placeholder="Apellido" autofocus="" name="apellido" value="<%=(typeof old != 'undefined' && old.apellido)?old.apellido: '' %>">

                        <small class="text-danger"><%= (typeof errors != 'undefined' && errors.email) ? errors.email.msg : '' %></small>

                        <input type="email" id="user-email" class="form-control" placeholder="Email" autofocus="" name="email" value="<%=(typeof old != 'undefined' && old.email)?old.email: '' %> ">

                        <small class="text-danger"><%= (typeof errors != 'undefined' && errors.pass) ? errors.pass.msg : '' %></small>

                        <input type="password" id="user-pass" class="form-control" placeholder="Contraseña" autofocus="" name="pass">

                        <small class="text-danger"><%= (typeof errors != 'undefined' && errors.passrepeat) ? errors.passrepeat.msg : '' %></small>

                        <input type="password" id="user-repeatpass" class="form-control" placeholder="Reingresá tu contraseña" autofocus="" name="passrepeat">


                        <div class="form-control">

                            <p class="avatar">Imagen de perfil</p>
                            <input type="file" accept="image/*" name="avatar">
                        </div>
                        <button class="btn btn-primary btn-block" type="submit"><i class="fas fa-user-plus"></i> Registrarse!</button>

                        <hr>

                        <!--<div class="social-login">
                            <button class="btn facebook-btn social-btn" type="button"><span><i class="fab fa-facebook-f"></i> Ingresar con Facebook</span> </button>
                        </div>
                        <div class="social-login">
                            <button class="btn google-btn social-btn" type="button"><span><i class="fab fa-google"></i> Ingresar con Google</span> </button>
                        </div>-->


                        <a href="/users/login" id="cancel_signup"><i class="fas fa-angle-left"></i> Atrás</a>
                    </form>
                </div>
            </div>



            <%- include('partials/footer') %>

                <!-- <script src="/javascripts/register-scripts.js"></script> -->
    </body>

</html>