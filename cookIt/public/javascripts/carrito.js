function $(elemento) {
    return document.querySelector(elemento)
}
window.addEventListener('load', function() {
    let cantidad = $("#cantidad");

    cantidad.addEventListener(function(){
    })
})